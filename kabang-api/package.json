{
  "name": "kabang-api",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "start": "bun run src/server.ts",
    "build-ui": "cd ../kabang-ui && bun run build && bun run ../kabang-api/scripts/copy-build.js",
    "build": "bun run build-ui",
    "seed": "bun run src/seed.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  }
}